import { View, Text, ScrollView } from "react-native";
import React from "react";

const LiveChatContent = () => {
  return (
    <ScrollView>
      <Text>LiveChatContent</Text>
    </ScrollView>
  );
};

export default LiveChatContent;
